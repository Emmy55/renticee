
import Dropdown from './Dropdown';

const Page2 = () => {
  const handleButtonClick = () => {
 
  };

  return (
    <div>
      
      <Dropdown buttonText="Max price" onClick={handleButtonClick} />
    
    </div>
  );
};

export default Page2;
